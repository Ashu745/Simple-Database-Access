<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
    <style>
        table,
        th,
        td {
            width: 70%;
            text-align: center;
        }

        h2 {
            text-align: center;
        }

        .add {
            margin-left: 74%;
            width: 140px;
            margin-bottom: 10px;
        }
    </style>
</head>

<body>
    <H2>LIST OF ALL USERS</H2>
    <form method="get" action="/user/add">
        <button class="add">Adduser</button>
    </form>
    <table border="1" align="center">
        <tr>
            <th>Id</th>
            <th>Username</th>
            <th>Email</th>
        </tr>
        <% for(user of users){ %>
            <tr>
                <td>
                    <%= user.id %>
                </td>
                <td>
                    <%= user.username %>
                </td>
                <td>
                    <%= user.email %>
                </td>
                <td>
                    <form method="get" action="/user/<%= user.id %>/edit">
                        <button>Edit</button>
                    </form>
                </td>
                <td>
                    <form method="get" action="/user/<%= user.id %>/delete">
                        <button>Delete</button>
                    </form>
                </td>
            </tr>
            <% } %>
    </table>
</body>

</html>